# TEST: concatting PP and RP throws error

# Beat accepts str, int, and float
# Beat does not accept x, y, z

# Note is not created out of midi range
# Note is created within range

# it does not accept beats of invalid length
# it does accept beats of valid length

# transposing PP keeps all attributes the same except pitch (i.e. length is the same)

# transposing melody keeps all attributes the same except pitch

# RP duration is calculated as expected

# MP.repitch fails is pitch is not right length


# MP.repeat and concat work as expected
#
